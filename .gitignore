# deps
node_modules/

# build output
dist/
dist-electron/
release/

# the vendored protoc binary is large & platform-specific — fetch it per-platform
# (see README); keep it out of git
vendor/protoc/

# misc
.DS_Store
*.log

# UI settings store (local data, not for git)
conduit-data.json
